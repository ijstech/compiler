{
    "name": "@ijstech/compiler",
    "version": "0.1.0",
    "main": "/dist/index.js",
    "types": "./types",
    "devDependencies": {
        "@ijstech/components": "file:packages/components",
        "@types/mocha": "9.0.0",
        "@types/node": "14.17",
        "mocha": "10.0.0",
        "ts-node": "10.1.0",
        "typescript": "4.3.5"
    },
    "scripts": {
        "build": "rm -rf types && rm -rf dist && tsc && tsc --P tsconfig.bundle.json && node tools/bundleDist",
        "build:lib": "rm -rf bundle && node tools/buildLib.js && tsc --P tsconfig.lib.json && rm -rf bundle && rm -rf src/lib.ts && node tools/bundleLib.js",
        "build:test": "rm -rf bundle && tsc --P tsconfig.test.json && node tools/bundleTest.js && rm -rf bundle",
        "serve": "node tools/serve",
        "test": "mocha --exit --require ts-node/register test/*.spec.ts"
    }
}