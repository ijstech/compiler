{
    "name": "dapp",
    "version": "0.1.0",
    "devDependencies": {
        "@ijstech/components": "^0.1.16",
        "typescript": "4.3.5"
    },
    "scripts": {
        "build": "rm -rf dist && ts-node tools/bundle.ts",
        "test": "node tools/serve"
    }
}
